version: '3.8'

services:
  mariadb:
    image: mariadb:10.11
    container_name: capstone-mariadb
    restart: always
    environment:
      # .env 파일의 변수를 환경변수로 주입 (MariaDB 공식 권장 변수명 사용)
      MARIADB_ROOT_PASSWORD: ${MARIA_ROOT_PASSWORD}
      MARIADB_DATABASE: ${MARIA_DATABASE}
      MARIADB_USER: ${MARIA_USER}
      MARIADB_PASSWORD: ${MARIA_PASSWORD}
      TZ: Asia/Seoul
    command:
      # 한글 데이터 깨짐 방지를 위한 기본 인코딩 설정 추가
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    ports:
      # 호스트포트:컨테이너포트
      - "${MARIA_PORT:-3306}:3306"
    volumes:
      - ./mariadb/data:/var/lib/mysql
      - ./mariadb/init:/docker-entrypoint-initdb.d
    networks:
      - capstone-net
    healthcheck:
      # 비밀번호 하드코딩 없이 MariaDB 내장 헬스체크 스크립트 사용
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  redis:
    image: redis:7-alpine
    container_name: capstone-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - capstone-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

  spring-app:
    build:
      context: ./spring
      dockerfile: Dockerfile
    container_name: capstone-spring
    ports:
      - "8080:8080"
    environment:
      # 수정: mysql -> mariadb 호스트 및 드라이버 사용
      SPRING_DATASOURCE_URL: jdbc:mariadb://mariadb:3306/capstone_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
      SPRING_DATASOURCE_USERNAME: root  # 직접 하드코딩
      SPRING_DATASOURCE_PASSWORD: 1234  # 직접 하드코딩
      SPRING_REDIS_HOST: redis
      SPRING_REDIS_PORT: 6379
      PYTHON_SERVICE_URL: http://python-app:8000
    depends_on:
      mariadb:  # 수정: mysql -> mariadb 로 이름 변경
        condition: service_healthy
      redis:
        condition: service_healthy
      python-app:
        condition: service_started
    networks:
      - capstone-net

  python-app:
    build:
      context: ./python
      dockerfile: Dockerfile
    container_name: capstone-python
    ports:
      - "8000:8000"
    environment:
      APP_ENV: ${APP_ENV}
      TRYON_MODE: ${TRYON_MODE}
      REDIS_URL: redis://redis:6379/0
      WORKSPACE_DIR: /app/workspace
      MOCK_RESULT_IMAGE: /app/workspace/mock/mock_result.jpg
      IDM_VTON_ROOT: /app/vton
    volumes:
      - ./python/workspace:/app/workspace
      - ./python/models:/app/models
      - ./python/mock:/app/workspace/mock
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - capstone-net

volumes:
  # mysql_data: 는 ./mariadb/data 바인드 마운트를 사용하므로 불필요하여 삭제했습니다.
  redis_data:

networks:
  capstone-net:
    driver: bridge
