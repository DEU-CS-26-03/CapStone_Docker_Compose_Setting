package com.capstone.tryon.python;public interface CatVtonClient {    // STEP 1: 추론 작업 제출 → pythonJobId 반환    String submitJob(String personPath, String clothPath, String clothType);    // STEP 2: 작업 상태 조회 → "DONE" | "FAILED" | "RUNNING"    String pollJobStatus(String jobId);    // STEP 3: 완료된 결과 이미지 byte[] 수신    byte[] fetchResultImage(String jobId);}